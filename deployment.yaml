apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-spring-backend
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: sample-spring-backend
  template:
    metadata:
      labels:
        app: sample-spring-backend
    spec:
      containers:
        - name: sample-spring-backend
          image: 291533180773.dkr.ecr.ap-southeast-2.amazonaws.com/sample-spring-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
